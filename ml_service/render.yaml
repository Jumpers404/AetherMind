services:
    - type: web
      name: keystroke-api
      env: python
  buildCommand: pip install --only-binary=:all: -r requirements.txt
      startCommand: uvicorn app.keystroke_api:app --host 0.0.0.0 --port 10000
